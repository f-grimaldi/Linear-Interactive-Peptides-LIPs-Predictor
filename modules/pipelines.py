# TODO Define main pipeline used by the program
def main_pipeline(config={}):
    return None


# TODO Define pipeline used for predictions
def predict_pipeline(config={}):
    return main_pipeline(config)


# TODO Define pipeline used for model training
def train_pipeline(config={}):
    return main_pipeline(config)
